// file path: src/index.js

import renderProfile from "./profile/renderProfile";
import "./index.css";

const profileData = {
  name: "Tomas",
  location: "The World",
};

renderProfile(profileData);

// alert("App is ready");
